import VueVideoTape from './tape.vue';

// VueVideoTape.install = Vue => Vue.component(VueVideoTape.name,VueVideoTape);

// if (typeof window !== 'undefined' && window.Vue) {
// 	window.Vue.use(VueVideoTape);
// }

export default VueVideoTape;